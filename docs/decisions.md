# Decision Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-20 | Recognisability threshold = 2 of 5 votes | The dataset authors' own definition of a poor image (Chiu et al., CVPR 2020) |
| 2026-09-20 | Drop the OTH flaw class | ~0.8% prevalence, reason unspecified, not learnable |
| 2026-09-20 | Classical (non-DL) baseline by design | Makes the CNN comparison meaningful rather than big-CNN vs small-CNN |
| 2026-09-25 | Train image loader must filter to the 23,431 IDs in train.json, not glob the images/train folder | Verified the raw train.zip pool contains 523 extra unannotated images (VizWiz_train_00023431.jpg-00023953.jpg, a contiguous block right after the last annotated ID, confirmed via a full disk-vs-annotation diff on the downloaded data) with zero missing annotated files - harmless, but must be excluded since they have no labels. UPDATE 2026-09-25 below: root cause of these extras is now confirmed, not just "harmless and unexplained" |
| 2026-09-25 | Test-split flaw labels are recoverable; recognisability is not | VizWiz_quality_issues_train_val_test.csv holds raw per-worker votes (IMG, WORKERID, per-flaw 0/1, REJECT, SPLIT) for all three splits, so per-flaw vote counts for test can be reconstructed by grouping on IMG and summing non-rejected rows - but the CSV has no unrecognizable column, so the recognisability head still has no test labels and needs the val-split-in-half fallback |
| 2026-09-25 | SUPERSEDES the row above: test-split flaw-label reconstruction from the CSV is REJECTED - do not use it | Built scripts/validate_reconstruction.py to prove the row above before relying on it. Found and fixed three real bugs (UTF-8 BOM breaking the IMG column key, a combined filename tricking split-inference into dumping all CSV rows into "train", and the CSV's 12-digit zero-padded IDs vs train.json's 8-digit IDs). After all three fixes, reconstruction still only reaches 331-365/23,431 (1.4-1.6%) exact matches on train and 0/7,750 (0%) on val. ROOT CAUSE CONFIRMED (not speculative) via https://vizwiz.org/tasks-and-datasets/vqa/: the CSV is annotation data from the DEPRECATED pre-2020 VizWiz release, which used 12-digit zero-padded filenames (e.g. VizWiz_val_000000028000.jpg) and different split sizes (20,000 train / 3,173 val) than the CURRENT release (8-digit filenames, e.g. VizWiz_val_00028000.jpg; 20,523 / 4,319) that our train.json/val.json/test.json come from. Different dataset versions = different image numbering; no crosswalk between the two is published anywhere. The 331-365 apparent train matches are coincidental collisions between two unrelated numbering schemes, NOT genuine reconstructions, and must NOT be salvaged - there is no way to distinguish a real match from a coincidental one after the fact, so using any of them risks silently training on wrong labels. Further crosswalk recovery (e.g. matching deprecated-to-current images by content) was not attempted: no mapping is published, and the fallback protocol (val split in half) was already the primary plan regardless of whether reconstruction succeeded. Consequence: test-split labels (both flaws and recognisability) are unavailable; the val-split-in-half fallback (fixed seed 42) is required for ALL heads, not recognisability alone |
| 2026-09-25 | CONFIRMED root cause of the 523 extra train images: they are privacy-redacted, not stray/unannotated | Per https://vizwiz.org/tasks-and-datasets/vqa/: "VizWiz_train_00023431.jpg - VizWiz_train_00023958.jpg are images which include private information that is obfuscated using the ImageNet mean." This is exactly our extra block. Note a small discrepancy we are logging honestly rather than rounding away: the documented range is 528 images (23431-23958 inclusive) but our own disk-vs-annotation diff found exactly 523 extra files (23431-23953) - 5 fewer than documented; not investigated further since it doesn't change the decision (all of it lies outside our annotated range regardless). The entire redacted range (23431-23958) lies OUTSIDE our annotated range (0-23430 local ids / VizWiz_train_00000000-00023430), so no training image used by this project contains redaction artifacts - the original exclusion decision (row above) stands, now with a confirmed rather than unknown reason |
